//==================================
// Create Time Difference interface
//==================================
interface TimeDifference {
    days?: number;
    hours?: number;
    minutes?: number;
    seconds?: number;
}

interface TimeRecord {
    start?: number; // date number
    end?: number; // date number
}